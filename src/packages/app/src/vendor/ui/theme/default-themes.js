/** @file Built-in theme definitions; now empty since only Bootstrap 5.3 color modes are used. */
// Legacy 37-theme JSONs removed. Bootstrap 5.3 color modes only.
/** Map of built-in theme id to theme definition; intentionally empty (Bootstrap color modes only). */
export const DEFAULT_THEMES = {};
