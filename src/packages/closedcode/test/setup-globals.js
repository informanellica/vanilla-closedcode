// Test setup file (sync, runs before test modules import). Reserved for runtime
// polyfills downstream test imports rely on at module-load time. (The former
// node:ffi polyfill was only needed by @opentui/core, which has been removed.)
