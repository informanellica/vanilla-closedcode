/**
 * @file PTY platform-abstraction entry point (placeholder).
 * Exposes the `#pty` import target; the concrete implementation is supplied
 * per-platform (see pty.node.js). This file intentionally exports nothing on
 * its own and exists so the module resolver has a stable shim.
 */
export {};