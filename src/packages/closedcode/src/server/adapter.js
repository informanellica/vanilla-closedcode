/**
 * @file Server runtime-adapter entry point (placeholder).
 * Resolves to the active runtime adapter (see adapter.express.js); this file
 * is a stable shim that exports nothing on its own.
 */
export {};